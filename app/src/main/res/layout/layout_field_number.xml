<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".fragments.createconfiguration.CreateConfigurationFragment">
    <data>
        <variable
            name="viewModel"
            type="edu.gtri.gpssample.viewmodels.ConfigurationViewModel" />

    </data>
    <LinearLayout
        android:id="@+id/fragment_root_layout"
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <CheckBox
            android:id="@+id/integer_only_checkBox"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="20dp"
            android:layout_marginTop="20dp"
            android:layout_marginEnd="20dp"
            android:text="Integer Only"
            android:textSize="18sp"
            android:onCheckedChanged="@{viewModel::onFieldIntegerOnlySelected}"/>

        <CheckBox
            android:id="@+id/pii_checkBox"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="20dp"
            android:layout_marginEnd="20dp"
            android:text="Personally Identifiable Information"
            android:textSize="18sp"
            android:onCheckedChanged="@{viewModel::onFieldPIISelected}"/>

        <CheckBox
            android:id="@+id/required_checkBox"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="20dp"
            android:layout_marginEnd="20dp"
            android:text="Required"
            android:textSize="18sp"
            android:onCheckedChanged="@{viewModel::onFieldRequiredSelected}"/>
    </LinearLayout>
</layout>